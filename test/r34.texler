# R3.4 [Aceptar] Un programa que aplique una función aritmética sobre cierto 
# campo del archivo de entrada.
function r34()
    File "path" as input.
    File "new" as output.

    with input: for line in lines().byIndex(1) do
                    for column in line.columns() do
                        if column is Number then
                            column * 2 -> output.
                        else
                            column -> output.
                        .
                    .
                .
    .

    return.
end

